// Shared RN components land here in S2 (Card, BigButton, LanternMeter). Kept as a package so imports are stable.
export const theme = {
  primary: "#FFB84D",
  cream: "#FFF3DC",
  ink: "#3b2a1a",
  brown: "#6b4a2b",
  radius: 24,
} as const;
